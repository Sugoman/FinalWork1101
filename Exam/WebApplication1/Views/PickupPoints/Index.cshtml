@model IEnumerable<Library.Models.PickupPoint>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PickupPointIndex)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PickupPointCity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PickupPointStreet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PickupPointHouse)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PickupPointIndex)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupPointCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupPointStreet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PickupPointHouse)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PickupPointId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PickupPointId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PickupPointId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
